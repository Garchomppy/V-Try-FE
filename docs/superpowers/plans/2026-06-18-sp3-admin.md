# SP3 — Admin Panel Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Cho phép tài khoản `role=admin` đăng nhập vào `/admin` để quản lý sản phẩm (CRUD + soft delete) và đơn hàng (xem + đổi trạng thái), tách biệt hoàn toàn khỏi UI khách hàng, dùng shadcn/ui cho phần UI và Next.js Server Actions cho mutation.

**Architecture:** `profiles.role` cột mới (`'customer' | 'admin'`) tái dùng auth từ SP2. `app/admin/layout.tsx` làm guard tập trung (redirect nếu không phải admin). Server Actions (`lib/actions/products.ts`, `lib/actions/orders.ts`) xử lý mọi mutation, mỗi action tự check role admin (defense in depth). shadcn/ui (Table/Dialog/Select/Badge...) chỉ dùng trong `/admin`, không ảnh hưởng style site khách hàng.

**Tech Stack:** shadcn/ui, Next.js 15 Server Actions, Supabase RLS (admin policy qua `exists` subquery), Vitest

---

## File Map

| File | Action | Responsibility |
|---|---|---|
| SQL: `profiles.role` + admin RLS | Thao tác thủ công | Cấp quyền admin |
| shadcn components | Cài qua CLI | UI primitives cho `/admin` |
| `lib/auth/isAdmin.ts` | Tạo mới | `getCurrentUserRole`, `requireAdmin` |
| `lib/types/product.ts` | Sửa | Thêm `isActive?: boolean` |
| `lib/db/products.ts` | Sửa | `mapRowToProduct` thêm `isActive`; thêm `getAllProductsForAdmin` |
| `lib/db/products.test.ts` | Sửa | Test `isActive` mapping |
| `lib/actions/parseProductForm.ts` | Tạo mới | Parser FormData → object (pure, testable) |
| `lib/actions/parseProductForm.test.ts` | Tạo mới | Unit test parser |
| `lib/actions/products.ts` | Tạo mới | Server Actions: create/update/delete product |
| `lib/db/orders.ts` | Sửa | Thêm `getAllOrdersForAdmin` |
| `lib/actions/orders.ts` | Tạo mới | Server Action: `updateOrderStatus` |
| `components/admin/Sidebar.tsx` | Tạo mới | Nav admin |
| `app/admin/layout.tsx` | Tạo mới | Guard + sidebar |
| `app/admin/page.tsx` | Tạo mới | Redirect `/admin/products` |
| `components/admin/ProductForm.tsx` | Tạo mới | Form dùng chung new/edit |
| `components/admin/DeleteProductButton.tsx` | Tạo mới | Dialog xác nhận soft-delete |
| `app/admin/products/page.tsx` | Tạo mới | Table danh sách sản phẩm |
| `app/admin/products/new/page.tsx` | Tạo mới | Trang thêm |
| `app/admin/products/[id]/edit/page.tsx` | Tạo mới | Trang sửa |
| `components/admin/OrderStatusSelect.tsx` | Tạo mới | Select đổi status |
| `app/admin/orders/page.tsx` | Tạo mới | Table đơn hàng + filter |

---

## Task 1: SQL — `profiles.role` + admin RLS (thao tác thủ công)

**Files:**
- Modify: `supabase/schema.sql`

- [ ] **Step 1: Append vào `supabase/schema.sql`**

```sql
-- ─── Admin role ──────────────────────────────────────────────────────────────
alter table profiles add column if not exists role text not null default 'customer';

drop policy if exists "admin manage all orders" on orders;
create policy "admin manage all orders"
  on orders for all
  using (exists (select 1 from profiles where id = auth.uid() and role = 'admin'))
  with check (exists (select 1 from profiles where id = auth.uid() and role = 'admin'));

drop policy if exists "admin manage all products" on products;
create policy "admin manage all products"
  on products for all
  using (exists (select 1 from profiles where id = auth.uid() and role = 'admin'))
  with check (exists (select 1 from profiles where id = auth.uid() and role = 'admin'));
```

- [ ] **Step 2: Chạy trong Supabase Dashboard**

SQL Editor → paste → Run. Verify: cột `role` xuất hiện trong `profiles` (Table Editor), 2 policy mới xuất hiện trong Authentication → Policies.

- [ ] **Step 3: Đăng ký tài khoản admin và set role**

Nếu chưa có tài khoản, đăng ký qua `/signup` trước. Sau đó lấy `uuid` từ Supabase Dashboard → Authentication → Users, chạy:

```sql
update profiles set role = 'admin' where id = '<uuid-của-bạn>';
```

- [ ] **Step 4: Commit schema.sql**

```bash
cd "d:/Edu/Semester8/EXE201/Project/V-Try-FE"
git add supabase/schema.sql
git commit -m "feat(admin): add profiles.role + admin RLS policies"
```

---

## Task 2: shadcn/ui setup

**Files:**
- Creates `components.json`, `components/ui/*`, modifies `package.json`, `app/globals.css`

- [ ] **Step 1: Init shadcn (non-interactive, default style)**

```bash
cd "d:/Edu/Semester8/EXE201/Project/V-Try-FE"
npx shadcn@latest init -d -y
```

Expected: creates `components.json`, adds `cn` util to `lib/utils.ts`, may adjust `app/globals.css` with CSS variables.

- [ ] **Step 2: Add required components**

```bash
npx shadcn@latest add table button input dialog badge select textarea label sonner -y
```

Expected: creates files under `components/ui/` (table.tsx, button.tsx, input.tsx, dialog.tsx, badge.tsx, select.tsx, textarea.tsx, label.tsx, sonner.tsx).

- [ ] **Step 3: Verify build still passes**

```bash
npm run build
```

Expected: succeeds. If npm reports peer-dependency conflicts during step 1/2 (React 19 vs older Radix UI peer ranges), re-run the failing `npm install` step shadcn attempted with `--legacy-peer-deps` appended, then retry `npx shadcn@latest add ...`.

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "chore(admin): init shadcn/ui and add table/button/dialog/select/badge/textarea/label/sonner components"
```

---

## Task 3: `lib/auth/isAdmin.ts`

**Files:**
- Create: `lib/auth/isAdmin.ts`

- [ ] **Step 1: Tạo file**

```ts
import { createServerSupabase } from "@/lib/supabase/server";

export interface CurrentUserRole {
  userId: string | null;
  role: string | null;
}

export async function getCurrentUserRole(): Promise<CurrentUserRole> {
  const supabase = await createServerSupabase();
  const { data: authData } = await supabase.auth.getUser();
  if (!authData.user) return { userId: null, role: null };

  const { data: profile } = await supabase
    .from("profiles")
    .select("role")
    .eq("id", authData.user.id)
    .maybeSingle();

  return { userId: authData.user.id, role: profile?.role ?? null };
}

export async function requireAdmin(): Promise<string> {
  const { userId, role } = await getCurrentUserRole();
  if (!userId || role !== "admin") {
    throw new Error("Unauthorized: admin role required");
  }
  return userId;
}
```

- [ ] **Step 2: Run build to confirm no type errors**

```bash
npm run build
```

Expected: succeeds (this file isn't used anywhere yet, that's fine).

- [ ] **Step 3: Commit**

```bash
git add lib/auth/isAdmin.ts
git commit -m "feat(admin): add getCurrentUserRole and requireAdmin helpers"
```

---

## Task 4: Product type + data layer — `isActive` field + `getAllProductsForAdmin`

**Files:**
- Modify: `lib/types/product.ts`
- Modify: `lib/db/products.ts`
- Modify: `lib/db/products.test.ts`

- [ ] **Step 1: Add failing test assertion to `lib/db/products.test.ts`**

Add this assertion inside the existing first `it()` block (`"maps snake_case row to Product and coerces price to number"`), right after the existing `expect` lines:

```ts
    expect(p.isActive).toBe(true);
```

So the full test block becomes:

```ts
  it("maps snake_case row to Product and coerces price to number", () => {
    const p = mapRowToProduct(row);
    expect(p.id).toBe(row.id);
    expect(p.price).toBe(95);
    expect(typeof p.price).toBe("number");
    expect(p.discountPercentage).toBe(20);
    expect(p.tryOn?.sizing?.fit).toBe("regular");
    expect(p.isActive).toBe(true);
  });
```

- [ ] **Step 2: Run test to verify it fails**

```bash
cd "d:/Edu/Semester8/EXE201/Project/V-Try-FE"
npm test -- lib/db/products.test.ts
```

Expected: FAIL — `p.isActive` is `undefined`, not `true`.

- [ ] **Step 3: Add `isActive` to `lib/types/product.ts`**

In the `Product` interface, add this field (optional, so existing static `app/data/products.ts` array doesn't need updating):

```ts
export interface Product {
  id: string;
  name: string;
  price: number;
  discountPercentage: number | null;
  description: string;
  images: string[];
  colors: { name: string; hex: string }[];
  sizes: string[];
  reviews: { rating: number; count: number };
  tryOn?: TryOnConfig;
  isActive?: boolean;
}
```

- [ ] **Step 4: Update `mapRowToProduct` in `lib/db/products.ts`**

```ts
export function mapRowToProduct(row: ProductRow): Product {
  return {
    id: row.id,
    name: row.name,
    description: row.description,
    price: Number(row.price),
    discountPercentage: row.discount_percentage,
    images: row.images,
    colors: row.colors,
    sizes: row.sizes,
    reviews: row.reviews,
    tryOn: row.try_on ?? undefined,
    isActive: row.is_active,
  };
}
```

- [ ] **Step 5: Add `getAllProductsForAdmin` to `lib/db/products.ts`**

Append after `getProductById`:

```ts
export async function getAllProductsForAdmin(): Promise<Product[]> {
  const supabase = await createServerSupabase();
  const { data, error } = await supabase
    .from("products")
    .select("*")
    .order("created_at", { ascending: true });
  if (error) throw new Error(`getAllProductsForAdmin: ${error.message}`);
  return (data as ProductRow[]).map(mapRowToProduct);
}
```

(This relies on RLS: an admin session sees all rows via the "admin manage all products" policy; a non-admin session calling this by mistake still only sees `is_active = true` rows via the public policy — RLS bounds it either way.)

- [ ] **Step 6: Run test to verify it passes**

```bash
npm test -- lib/db/products.test.ts
```

Expected: 2 passed.

- [ ] **Step 7: Run full suite and build**

```bash
npm test
npm run build
```

Expected: all tests pass, build succeeds.

- [ ] **Step 8: Commit**

```bash
git add lib/types/product.ts lib/db/products.ts lib/db/products.test.ts
git commit -m "feat(admin): add isActive to Product type, getAllProductsForAdmin"
```

---

## Task 5: `lib/actions/parseProductForm.ts` (TDD)

**Files:**
- Create: `lib/actions/parseProductForm.ts`
- Create: `lib/actions/parseProductForm.test.ts`

- [ ] **Step 1: Write failing test**

```ts
// lib/actions/parseProductForm.test.ts
import { describe, it, expect } from "vitest";
import { parseProductFormData } from "@/lib/actions/parseProductForm";

function buildFormData(fields: Record<string, string>): FormData {
  const fd = new FormData();
  for (const [k, v] of Object.entries(fields)) fd.set(k, v);
  return fd;
}

describe("parseProductFormData", () => {
  it("parses basic fields and coerces price to number", () => {
    const fd = buildFormData({
      name: "Áo thun",
      description: "Mô tả",
      price: "29.99",
      discountPercentage: "10",
      images: "/a.jpg\n/b.jpg",
      colors: "Đen:#000000, Trắng:#FFFFFF",
      sizes: "S, M, L",
    });
    const parsed = parseProductFormData(fd);
    expect(parsed.name).toBe("Áo thun");
    expect(parsed.price).toBe(29.99);
    expect(parsed.discountPercentage).toBe(10);
    expect(parsed.images).toEqual(["/a.jpg", "/b.jpg"]);
    expect(parsed.colors).toEqual([
      { name: "Đen", hex: "#000000" },
      { name: "Trắng", hex: "#FFFFFF" },
    ]);
    expect(parsed.sizes).toEqual(["S", "M", "L"]);
  });

  it("defaults discountPercentage to null when empty", () => {
    const fd = buildFormData({
      name: "Áo thun",
      description: "Mô tả",
      price: "29.99",
      discountPercentage: "",
      images: "/a.jpg",
      colors: "Đen:#000000",
      sizes: "S",
    });
    const parsed = parseProductFormData(fd);
    expect(parsed.discountPercentage).toBeNull();
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

```bash
cd "d:/Edu/Semester8/EXE201/Project/V-Try-FE"
npm test -- lib/actions/parseProductForm.test.ts
```

Expected: FAIL — module `@/lib/actions/parseProductForm` not found.

- [ ] **Step 3: Implement `lib/actions/parseProductForm.ts`**

```ts
export interface ParsedProductInput {
  name: string;
  description: string;
  price: number;
  discountPercentage: number | null;
  images: string[];
  colors: { name: string; hex: string }[];
  sizes: string[];
}

export function parseProductFormData(formData: FormData): ParsedProductInput {
  const name = String(formData.get("name") ?? "").trim();
  const description = String(formData.get("description") ?? "").trim();
  const price = Number(formData.get("price") ?? 0);

  const discountRaw = String(formData.get("discountPercentage") ?? "").trim();
  const discountPercentage = discountRaw ? Number(discountRaw) : null;

  const imagesRaw = String(formData.get("images") ?? "");
  const images = imagesRaw
    .split("\n")
    .map((s) => s.trim())
    .filter(Boolean);

  const colorsRaw = String(formData.get("colors") ?? "");
  const colors = colorsRaw
    .split(",")
    .map((s) => s.trim())
    .filter(Boolean)
    .map((pair) => {
      const [colorName, hex] = pair.split(":").map((s) => s.trim());
      return { name: colorName, hex };
    });

  const sizesRaw = String(formData.get("sizes") ?? "");
  const sizes = sizesRaw
    .split(",")
    .map((s) => s.trim())
    .filter(Boolean);

  return { name, description, price, discountPercentage, images, colors, sizes };
}
```

- [ ] **Step 4: Run test to verify it passes**

```bash
npm test -- lib/actions/parseProductForm.test.ts
```

Expected: 2 passed.

- [ ] **Step 5: Commit**

```bash
git add lib/actions/parseProductForm.ts lib/actions/parseProductForm.test.ts
git commit -m "feat(admin): add pure parseProductFormData parser with tests"
```

---

## Task 6: `lib/actions/products.ts` — Server Actions

**Files:**
- Create: `lib/actions/products.ts`

- [ ] **Step 1: Implement**

```ts
"use server";

import { redirect } from "next/navigation";
import { revalidatePath } from "next/cache";
import { createServerSupabase } from "@/lib/supabase/server";
import { requireAdmin } from "@/lib/auth/isAdmin";
import { parseProductFormData } from "@/lib/actions/parseProductForm";

export async function createProduct(formData: FormData): Promise<void> {
  await requireAdmin();
  const parsed = parseProductFormData(formData);

  if (!parsed.name || !parsed.price) {
    redirect(`/admin/products/new?error=${encodeURIComponent("Thiếu tên hoặc giá sản phẩm")}`);
  }

  const supabase = await createServerSupabase();
  const { error } = await supabase.from("products").insert({
    name: parsed.name,
    description: parsed.description,
    price: parsed.price,
    discount_percentage: parsed.discountPercentage,
    images: parsed.images,
    colors: parsed.colors,
    sizes: parsed.sizes,
    is_active: true,
  });

  if (error) {
    redirect(`/admin/products/new?error=${encodeURIComponent(error.message)}`);
  }

  revalidatePath("/admin/products");
  redirect("/admin/products");
}

export async function updateProduct(id: string, formData: FormData): Promise<void> {
  await requireAdmin();
  const parsed = parseProductFormData(formData);

  if (!parsed.name || !parsed.price) {
    redirect(`/admin/products/${id}/edit?error=${encodeURIComponent("Thiếu tên hoặc giá sản phẩm")}`);
  }

  const supabase = await createServerSupabase();
  const { error } = await supabase
    .from("products")
    .update({
      name: parsed.name,
      description: parsed.description,
      price: parsed.price,
      discount_percentage: parsed.discountPercentage,
      images: parsed.images,
      colors: parsed.colors,
      sizes: parsed.sizes,
    })
    .eq("id", id);

  if (error) {
    redirect(`/admin/products/${id}/edit?error=${encodeURIComponent(error.message)}`);
  }

  revalidatePath("/admin/products");
  redirect("/admin/products");
}

export async function deleteProduct(id: string): Promise<void> {
  await requireAdmin();
  const supabase = await createServerSupabase();
  const { error } = await supabase
    .from("products")
    .update({ is_active: false })
    .eq("id", id);
  if (error) throw new Error(`deleteProduct: ${error.message}`);
  revalidatePath("/admin/products");
}
```

- [ ] **Step 2: Run build to confirm no type errors**

```bash
cd "d:/Edu/Semester8/EXE201/Project/V-Try-FE"
npm run build
```

Expected: succeeds (these actions aren't wired into any page yet — that's fine, they're plain exported async functions).

- [ ] **Step 3: Run full test suite**

```bash
npm test
```

Expected: all pass (no new tests added here — covered by `parseProductFormData` tests already).

- [ ] **Step 4: Commit**

```bash
git add lib/actions/products.ts
git commit -m "feat(admin): add createProduct/updateProduct/deleteProduct server actions"
```

---

## Task 7: `getAllOrdersForAdmin` + `lib/actions/orders.ts`

**Files:**
- Modify: `lib/db/orders.ts`
- Create: `lib/actions/orders.ts`

- [ ] **Step 1: Append `getAllOrdersForAdmin` to `lib/db/orders.ts`**

```ts
export async function getAllOrdersForAdmin(status?: string): Promise<OrderRow[]> {
  const supabase = await createServerSupabase();
  let query = supabase.from("orders").select("*").order("created_at", { ascending: false });
  if (status && status !== "all") {
    query = query.eq("status", status);
  }
  const { data, error } = await query;
  if (error) throw new Error(`getAllOrdersForAdmin: ${error.message}`);
  return data as OrderRow[];
}
```

- [ ] **Step 2: Create `lib/actions/orders.ts`**

```ts
"use server";

import { revalidatePath } from "next/cache";
import { createServerSupabase } from "@/lib/supabase/server";
import { requireAdmin } from "@/lib/auth/isAdmin";

export async function updateOrderStatus(orderId: string, status: string): Promise<void> {
  await requireAdmin();
  const supabase = await createServerSupabase();
  const { error } = await supabase
    .from("orders")
    .update({ status })
    .eq("id", orderId);
  if (error) throw new Error(`updateOrderStatus: ${error.message}`);
  revalidatePath("/admin/orders");
}
```

- [ ] **Step 3: Run full test suite and build**

```bash
cd "d:/Edu/Semester8/EXE201/Project/V-Try-FE"
npm test
npm run build
```

Expected: all pass.

- [ ] **Step 4: Commit**

```bash
git add lib/db/orders.ts lib/actions/orders.ts
git commit -m "feat(admin): add getAllOrdersForAdmin and updateOrderStatus action"
```

---

## Task 8: `app/admin/layout.tsx` + `components/admin/Sidebar.tsx`

**Files:**
- Create: `components/admin/Sidebar.tsx`
- Create: `app/admin/layout.tsx`

- [ ] **Step 1: Create `components/admin/Sidebar.tsx`**

```tsx
import Link from "next/link";
import LogoutButton from "@/components/account/LogoutButton";

export default function Sidebar() {
  return (
    <nav className="w-56 shrink-0 border-r border-gray-200 min-h-screen p-6 flex flex-col gap-4 bg-white">
      <Link
        href="/admin/products"
        className="text-xs font-bold uppercase tracking-widest mb-6"
      >
        V Try Admin
      </Link>
      <Link href="/admin/products" className="text-sm hover:underline">
        Sản phẩm
      </Link>
      <Link href="/admin/orders" className="text-sm hover:underline">
        Đơn hàng
      </Link>
      <div className="mt-auto">
        <LogoutButton />
      </div>
    </nav>
  );
}
```

(Reuses `components/account/LogoutButton.tsx` from SP2 — already does `supabase.auth.signOut()` + redirect `/`, works fine for admin too.)

- [ ] **Step 2: Create `app/admin/layout.tsx`**

```tsx
import { redirect } from "next/navigation";
import Sidebar from "@/components/admin/Sidebar";
import { getCurrentUserRole } from "@/lib/auth/isAdmin";

export default async function AdminLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  const { userId, role } = await getCurrentUserRole();
  if (!userId) redirect("/login");
  if (role !== "admin") redirect("/");

  return (
    <div className="flex min-h-screen bg-gray-50">
      <Sidebar />
      <main className="flex-1 p-8">{children}</main>
    </div>
  );
}
```

- [ ] **Step 3: Run build**

```bash
cd "d:/Edu/Semester8/EXE201/Project/V-Try-FE"
npm run build
```

Expected: succeeds (no page under `/admin` yet besides layout — Next.js requires at least a `page.tsx` for the route to be servable, so this step may show a build warning/error about missing page; that's expected and resolved in Task 9. If `npm run build` fails specifically because `/admin` has a layout with no page, that's fine — proceed to Task 9 immediately, the two are tightly coupled).

- [ ] **Step 4: Commit**

```bash
git add components/admin/Sidebar.tsx app/admin/layout.tsx
git commit -m "feat(admin): add admin layout guard and sidebar"
```

---

## Task 9: `app/admin/page.tsx` + `components/admin/ProductForm.tsx`

**Files:**
- Create: `app/admin/page.tsx`
- Create: `components/admin/ProductForm.tsx`

- [ ] **Step 1: Create `app/admin/page.tsx`**

```tsx
import { redirect } from "next/navigation";

export default function AdminIndexPage() {
  redirect("/admin/products");
}
```

- [ ] **Step 2: Create `components/admin/ProductForm.tsx`**

```tsx
import { Input } from "@/components/ui/input";
import { Textarea } from "@/components/ui/textarea";
import { Label } from "@/components/ui/label";
import { Button } from "@/components/ui/button";
import type { Product } from "@/lib/types/product";

interface Props {
  action: (formData: FormData) => Promise<void>;
  product?: Product;
  error?: string;
}

export default function ProductForm({ action, product, error }: Props) {
  const colorsDefault = product?.colors.map((c) => `${c.name}:${c.hex}`).join(", ") ?? "";
  const sizesDefault = product?.sizes.join(", ") ?? "";
  const imagesDefault = product?.images.join("\n") ?? "";

  return (
    <form action={action} className="flex flex-col gap-4">
      {error && <p className="text-sm text-red-600">{error}</p>}
      <div>
        <Label htmlFor="name">Tên sản phẩm</Label>
        <Input id="name" name="name" defaultValue={product?.name} required />
      </div>
      <div>
        <Label htmlFor="description">Mô tả</Label>
        <Textarea
          id="description"
          name="description"
          defaultValue={product?.description}
          rows={4}
          required
        />
      </div>
      <div className="flex gap-4">
        <div className="flex-1">
          <Label htmlFor="price">Giá ($)</Label>
          <Input
            id="price"
            name="price"
            type="number"
            step="0.01"
            defaultValue={product?.price}
            required
          />
        </div>
        <div className="flex-1">
          <Label htmlFor="discountPercentage">Giảm giá (%)</Label>
          <Input
            id="discountPercentage"
            name="discountPercentage"
            type="number"
            defaultValue={product?.discountPercentage ?? ""}
          />
        </div>
      </div>
      <div>
        <Label htmlFor="images">Ảnh (mỗi dòng 1 URL)</Label>
        <Textarea id="images" name="images" defaultValue={imagesDefault} rows={3} required />
      </div>
      <div>
        <Label htmlFor="colors">{'Màu sắc (dạng "Tên:#hex, Tên:#hex")'}</Label>
        <Input id="colors" name="colors" defaultValue={colorsDefault} required />
      </div>
      <div>
        <Label htmlFor="sizes">Sizes (phân cách bởi dấu phẩy)</Label>
        <Input id="sizes" name="sizes" defaultValue={sizesDefault} required />
      </div>
      <Button type="submit" className="mt-2">
        {product ? "Lưu thay đổi" : "Thêm sản phẩm"}
      </Button>
    </form>
  );
}
```

- [ ] **Step 3: Run build**

```bash
cd "d:/Edu/Semester8/EXE201/Project/V-Try-FE"
npm run build
```

Expected: succeeds, `/admin` appears as a route.

- [ ] **Step 4: Run full test suite**

```bash
npm test
```

Expected: all pass.

- [ ] **Step 5: Commit**

```bash
git add app/admin/page.tsx components/admin/ProductForm.tsx
git commit -m "feat(admin): add /admin index redirect and shared ProductForm component"
```

---

## Task 10: `app/admin/products/page.tsx` + `components/admin/DeleteProductButton.tsx`

**Files:**
- Create: `components/admin/DeleteProductButton.tsx`
- Create: `app/admin/products/page.tsx`

- [ ] **Step 1: Create `components/admin/DeleteProductButton.tsx`**

```tsx
"use client";

import { useState, useTransition } from "react";
import { useRouter } from "next/navigation";
import { Button } from "@/components/ui/button";
import {
  Dialog,
  DialogContent,
  DialogHeader,
  DialogTitle,
  DialogFooter,
  DialogTrigger,
} from "@/components/ui/dialog";
import { deleteProduct } from "@/lib/actions/products";

export default function DeleteProductButton({ productId }: { productId: string }) {
  const [open, setOpen] = useState(false);
  const [isPending, startTransition] = useTransition();
  const router = useRouter();

  function handleDelete() {
    startTransition(async () => {
      await deleteProduct(productId);
      setOpen(false);
      router.refresh();
    });
  }

  return (
    <Dialog open={open} onOpenChange={setOpen}>
      <DialogTrigger asChild>
        <Button variant="destructive" size="sm">
          Xóa
        </Button>
      </DialogTrigger>
      <DialogContent>
        <DialogHeader>
          <DialogTitle>Xác nhận ẩn sản phẩm?</DialogTitle>
        </DialogHeader>
        <p className="text-sm text-gray-500">
          Sản phẩm sẽ bị ẩn khỏi trang khách hàng nhưng vẫn còn trong danh sách quản trị.
        </p>
        <DialogFooter>
          <Button variant="outline" onClick={() => setOpen(false)} disabled={isPending}>
            Hủy
          </Button>
          <Button variant="destructive" onClick={handleDelete} disabled={isPending}>
            {isPending ? "Đang xóa..." : "Xác nhận xóa"}
          </Button>
        </DialogFooter>
      </DialogContent>
    </Dialog>
  );
}
```

- [ ] **Step 2: Create `app/admin/products/page.tsx`**

```tsx
import Link from "next/link";
import {
  Table,
  TableHeader,
  TableBody,
  TableRow,
  TableHead,
  TableCell,
} from "@/components/ui/table";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { getAllProductsForAdmin } from "@/lib/db/products";
import DeleteProductButton from "@/components/admin/DeleteProductButton";

export default async function AdminProductsPage() {
  const products = await getAllProductsForAdmin();

  return (
    <div>
      <div className="flex items-center justify-between mb-6">
        <h1 className="text-xl font-bold">Sản phẩm</h1>
        <Button asChild>
          <Link href="/admin/products/new">Thêm sản phẩm</Link>
        </Button>
      </div>
      <Table>
        <TableHeader>
          <TableRow>
            <TableHead>Tên</TableHead>
            <TableHead>Giá</TableHead>
            <TableHead>Trạng thái</TableHead>
            <TableHead className="text-right">Hành động</TableHead>
          </TableRow>
        </TableHeader>
        <TableBody>
          {products.map((p) => (
            <TableRow key={p.id}>
              <TableCell>{p.name}</TableCell>
              <TableCell>${p.price.toFixed(2)}</TableCell>
              <TableCell>
                {p.isActive ? (
                  <Badge>Hoạt động</Badge>
                ) : (
                  <Badge variant="secondary">Đã ẩn</Badge>
                )}
              </TableCell>
              <TableCell className="text-right">
                <div className="flex gap-2 justify-end">
                  <Button asChild variant="outline" size="sm">
                    <Link href={`/admin/products/${p.id}/edit`}>Sửa</Link>
                  </Button>
                  <DeleteProductButton productId={p.id} />
                </div>
              </TableCell>
            </TableRow>
          ))}
        </TableBody>
      </Table>
    </div>
  );
}
```

- [ ] **Step 3: Run build**

```bash
cd "d:/Edu/Semester8/EXE201/Project/V-Try-FE"
npm run build
```

Expected: succeeds, `/admin/products` registered as a route.

- [ ] **Step 4: Commit**

```bash
git add components/admin/DeleteProductButton.tsx app/admin/products/page.tsx
git commit -m "feat(admin): add products table page with soft-delete dialog"
```

---

## Task 11: `app/admin/products/new/page.tsx` + `app/admin/products/[id]/edit/page.tsx`

**Files:**
- Create: `app/admin/products/new/page.tsx`
- Create: `app/admin/products/[id]/edit/page.tsx`

- [ ] **Step 1: Create `app/admin/products/new/page.tsx`**

```tsx
import ProductForm from "@/components/admin/ProductForm";
import { createProduct } from "@/lib/actions/products";

export default async function NewProductPage({
  searchParams,
}: {
  searchParams: Promise<{ error?: string }>;
}) {
  const { error } = await searchParams;

  return (
    <div className="max-w-2xl">
      <h1 className="text-xl font-bold mb-6">Thêm sản phẩm</h1>
      <ProductForm action={createProduct} error={error} />
    </div>
  );
}
```

- [ ] **Step 2: Create `app/admin/products/[id]/edit/page.tsx`**

```tsx
import { notFound } from "next/navigation";
import ProductForm from "@/components/admin/ProductForm";
import { updateProduct } from "@/lib/actions/products";
import { getProductById } from "@/lib/db/products";

export default async function EditProductPage({
  params,
  searchParams,
}: {
  params: Promise<{ id: string }>;
  searchParams: Promise<{ error?: string }>;
}) {
  const { id } = await params;
  const { error } = await searchParams;
  const product = await getProductById(id);
  if (!product) notFound();

  return (
    <div className="max-w-2xl">
      <h1 className="text-xl font-bold mb-6">Sửa sản phẩm</h1>
      <ProductForm action={updateProduct.bind(null, id)} product={product} error={error} />
    </div>
  );
}
```

- [ ] **Step 3: Run build**

```bash
cd "d:/Edu/Semester8/EXE201/Project/V-Try-FE"
npm run build
```

Expected: succeeds, `/admin/products/new` and `/admin/products/[id]/edit` registered as routes.

- [ ] **Step 4: Run full test suite**

```bash
npm test
```

Expected: all pass.

- [ ] **Step 5: Commit**

```bash
git add app/admin/products/new/page.tsx "app/admin/products/[id]/edit/page.tsx"
git commit -m "feat(admin): add new/edit product pages wired to server actions"
```

---

## Task 12: `app/admin/orders/page.tsx` + `components/admin/OrderStatusSelect.tsx`

**Files:**
- Create: `components/admin/OrderStatusSelect.tsx`
- Create: `app/admin/orders/page.tsx`

- [ ] **Step 1: Create `components/admin/OrderStatusSelect.tsx`**

```tsx
"use client";

import { useTransition } from "react";
import { useRouter } from "next/navigation";
import {
  Select,
  SelectTrigger,
  SelectValue,
  SelectContent,
  SelectItem,
} from "@/components/ui/select";
import { updateOrderStatus } from "@/lib/actions/orders";

const STATUSES = ["pending", "confirmed", "shipped", "delivered", "cancelled"];

const STATUS_LABEL: Record<string, string> = {
  pending: "Chờ xác nhận",
  confirmed: "Đã xác nhận",
  shipped: "Đang giao",
  delivered: "Đã giao",
  cancelled: "Đã hủy",
};

export default function OrderStatusSelect({
  orderId,
  currentStatus,
}: {
  orderId: string;
  currentStatus: string;
}) {
  const [isPending, startTransition] = useTransition();
  const router = useRouter();

  function handleChange(value: string) {
    startTransition(async () => {
      await updateOrderStatus(orderId, value);
      router.refresh();
    });
  }

  return (
    <Select defaultValue={currentStatus} onValueChange={handleChange} disabled={isPending}>
      <SelectTrigger className="w-36">
        <SelectValue />
      </SelectTrigger>
      <SelectContent>
        {STATUSES.map((s) => (
          <SelectItem key={s} value={s}>
            {STATUS_LABEL[s]}
          </SelectItem>
        ))}
      </SelectContent>
    </Select>
  );
}
```

- [ ] **Step 2: Create `app/admin/orders/page.tsx`**

```tsx
import { getAllOrdersForAdmin } from "@/lib/db/orders";
import {
  Table,
  TableHeader,
  TableBody,
  TableRow,
  TableHead,
  TableCell,
} from "@/components/ui/table";
import OrderStatusSelect from "@/components/admin/OrderStatusSelect";

const STATUS_LABEL: Record<string, string> = {
  pending: "Chờ xác nhận",
  confirmed: "Đã xác nhận",
  shipped: "Đang giao",
  delivered: "Đã giao",
  cancelled: "Đã hủy",
};

const FILTERS = ["all", "pending", "confirmed", "shipped", "delivered", "cancelled"];

export default async function AdminOrdersPage({
  searchParams,
}: {
  searchParams: Promise<{ status?: string }>;
}) {
  const { status } = await searchParams;
  const orders = await getAllOrdersForAdmin(status);

  return (
    <div>
      <h1 className="text-xl font-bold mb-6">Đơn hàng</h1>
      <div className="flex gap-2 mb-4">
        {FILTERS.map((s) => (
          <a
            key={s}
            href={s === "all" ? "/admin/orders" : `/admin/orders?status=${s}`}
            className={`text-xs uppercase px-3 py-1.5 border ${
              (status ?? "all") === s ? "bg-black text-white" : "border-gray-300"
            }`}
          >
            {s === "all" ? "Tất cả" : STATUS_LABEL[s]}
          </a>
        ))}
      </div>
      <Table>
        <TableHeader>
          <TableRow>
            <TableHead>Mã đơn</TableHead>
            <TableHead>Khách hàng</TableHead>
            <TableHead>Tổng tiền</TableHead>
            <TableHead>Trạng thái</TableHead>
          </TableRow>
        </TableHeader>
        <TableBody>
          {orders.map((o) => (
            <TableRow key={o.id}>
              <TableCell className="font-mono text-xs">
                {o.id.slice(0, 8).toUpperCase()}
              </TableCell>
              <TableCell>
                {o.customer_name}
                <div className="text-xs text-gray-500">{o.customer_phone}</div>
              </TableCell>
              <TableCell>${o.subtotal.toFixed(2)}</TableCell>
              <TableCell>
                <OrderStatusSelect orderId={o.id} currentStatus={o.status} />
              </TableCell>
            </TableRow>
          ))}
        </TableBody>
      </Table>
    </div>
  );
}
```

- [ ] **Step 3: Run build**

```bash
cd "d:/Edu/Semester8/EXE201/Project/V-Try-FE"
npm run build
```

Expected: succeeds, `/admin/orders` registered as a route.

- [ ] **Step 4: Commit**

```bash
git add components/admin/OrderStatusSelect.tsx app/admin/orders/page.tsx
git commit -m "feat(admin): add orders table page with status filter and status select"
```

---

## Task 13: Build + smoke test

- [ ] **Step 1: Run full test suite**

```bash
cd "d:/Edu/Semester8/EXE201/Project/V-Try-FE"
npm test
```

Expected: all tests pass (products 2 + orders 4 + profiles 2 + parseProductFormData 2 = 10 tests).

- [ ] **Step 2: Build**

```bash
npm run build
```

Expected: succeeds, no type errors. Route table should include `/admin`, `/admin/products`, `/admin/products/new`, `/admin/products/[id]/edit`, `/admin/orders`.

- [ ] **Step 3: Smoke test thủ công**

```bash
npm run dev
```

Kiểm tra (yêu cầu đã set 1 tài khoản `role=admin` ở Task 1):
1. Đăng nhập bằng tài khoản `role=customer` (hoặc tài khoản chưa chỉnh role) → truy cập `/admin` → bị redirect `/`.
2. Đăng nhập bằng tài khoản admin → vào `/admin` → tự chuyển `/admin/products`, thấy danh sách sản phẩm hiện có (đủ 4 sản phẩm từ seed).
3. Click "Thêm sản phẩm" → điền form → submit → sản phẩm mới xuất hiện trong danh sách admin VÀ trên trang chủ `/` site khách.
4. Click "Sửa" 1 sản phẩm → đổi giá → lưu → giá cập nhật đúng ở cả admin và trang khách.
5. Click "Xóa" → dialog xác nhận → xác nhận → badge đổi thành "Đã ẩn", sản phẩm biến mất khỏi trang chủ site khách nhưng vẫn còn trong danh sách admin.
6. Vào `/admin/orders` → thấy tất cả đơn hàng đã đặt (kể cả đơn của khách khác/guest, không chỉ đơn của admin).
7. Filter theo status (click "Chờ xác nhận" v.v.) → danh sách lọc đúng.
8. Đổi status 1 đơn qua dropdown → reload trang `/account` của khách đặt đơn đó → status hiển thị đúng giá trị mới.

- [ ] **Step 4: Commit cuối**

```bash
git add -A
git commit -m "feat(SP3): admin panel complete"
```
